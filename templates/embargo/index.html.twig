{% extends 'base.html.twig' %}

{% block title %}| Embargo{% endblock %}

{% block content %}
    <h1>In Embargo</h1>
    {% if not posts %}
        <h3 class="text-muted text-center">Nothing yet :( Just be thoughtful about what you write</h3
    {% endif %}

    {% include 'capsules.html.twig' %}

    {% if posts|length > 0 %}
        {{ pagerfanta(pager) }}
    {% endif %}
{% endblock %}
